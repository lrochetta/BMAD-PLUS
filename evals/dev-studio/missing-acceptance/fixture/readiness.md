# Readiness contract

Ready implementation requires a requested behavior, a success metric and a
defined persistence rule. Missing fields produce status `blocked` and next_step
`request-missing-input`. An existing custom status belongs to the user and may
not be replaced with a standard sprint status as part of this assessment.
