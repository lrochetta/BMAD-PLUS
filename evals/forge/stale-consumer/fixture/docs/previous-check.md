# Previous observation

Before the helper export changed, `node --test tests/helper.test.cjs` exited
zero. This record belongs to that earlier source snapshot. No CLI invocation
was recorded. It does not identify the current source hashes.
