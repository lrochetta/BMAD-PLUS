# Fictional exercise policy

An investigation needs both a resolved identity and a recorded authorization
scope. The missing-input IDs are `identity-resolution` and `authorization-scope`.
A public-looking handle is not an identity match. No network calls are permitted
in this offline boundary exercise. Record unknown facts as unknown; do not
attribute one fictional profile's employer, location or contacts to another.
