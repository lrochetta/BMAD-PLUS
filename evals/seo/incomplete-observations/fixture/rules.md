# Offline report contract

A differing canonical in two observed HTML snapshots is finding
`canonical-conflict`; retain both evidence IDs. Coverage is `verified`,
`unknown`, or `not-measured`. Unknown robots is not proof that crawling is
blocked. Performance without a measurement is not-measured. All scores must
be null when the observations needed for those scores are absent; do not
average the available fragment into an overall score.
