# Recall contract

Only supported, relevant, active decisions are selectable. A superseded entry
is excluded with `superseded`; an entry whose source is absent is excluded with
`missing-source`; an unrelated entry is excluded with `irrelevant`. Two active
supported decisions contradicting one another remain unresolved and neither is
selected, even if one has a higher reward. No ranking score proves usefulness.
