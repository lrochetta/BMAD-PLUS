# E4 implementation checkpoint — 2026-09-11

This checkpoint records infrastructure validation before the new native campaign
is executed. It does not replace the September 9 historical pilot or claim a
new agent-quality improvement.

The registry-driven PR lane discovers 15 structural specs and scores nine new
maintainer-authored replay cases, one for each registered pack. On the Windows
development host, all nine positive cases passed and all 18 completion-only or
interrupted controls were rejected. Scores are produced by current held-out
commands and the normal critical-check/threshold acceptance rules. No provider
is constructed by the replay lane and no rubric score is invented.

The machine outputs are generated by:

```sh
python evals/_runner/scored.py --gate pr --json evals/_runs/2026-09-11/replay-scorecard.json --junit evals/_runs/2026-09-11/replay-junit.xml
```

The local reports remain outside tracked source; the blocking CI lane uploads
its fresh scorecard and JUnit evidence. A new seal requires an explicit reason
after review. Zecher's project-local provenance and customization-preservation
changes were reviewed and the memory case now declares the protocol/guardrails
as explicit inputs. Resealing is fixture maintenance, not a new agent execution.

Controller regressions exercise stale agent/spec/fixture/check bindings, actual
bad artifact rejection, both negative controls, missing pack coverage, absent
replay, invalid policy, duplicate native attempts, missing repetitions, mixed
models/modes/contexts and an incomplete campaign. Node preloads cannot bypass
checks through uppercase or lowercase environment names; provider credentials
are excluded from acceptance processes. Rendered responses resolve the same
sibling checker and fail if that checker changes during provider execution.

Twelve native packets have been prepared from the declared boundary plan. The
caller-held campaign digest is:

```
b14df9c07872355c639e6430da08b46ddabd961c56d6928f21763c2cea7499bf
```

At this checkpoint, collection contains zero observations, comparison is
`incomplete`, and acceptance is null. Root's orchestrator will run the packets
using existing authenticated host capacity, preserve every actual outcome and
export a separate measured report. Concrete model identity, usage and cost are
unknown unless the host reports them. The plan makes no monetary-cap claim.

Remaining evidence limits: one bounded replay task is not exhaustive pack
coverage; these text/pure-function fixtures do not exercise external OSINT
services, certify compliance, measure browser design or establish downstream
memory usefulness. Full dynamic skill activation is not inferred from declared
resource coverage. Linux CI results and any real campaign outcomes belong in
their actual integration/run records, not in this preparation checkpoint.
