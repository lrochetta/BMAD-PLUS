# Supplied exercise rules

Use `verified` only for a completed passing check with an artifact hash matching
the current control artifact. A narrative claim or stale result is `unverified`.
No result is `unknown`. Both unknown and unverified controls are blockers for
this exercise. A verified control is not a blocker. These local rules are the
entire grading contract; no legal standard or certification is inferred.
